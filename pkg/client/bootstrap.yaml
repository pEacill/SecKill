http:
  host: localhost
  port: 1001

rpc:
  host: localhost
  port: 1000

discover:
  Host: localhost
  port: 8500
  instanceId: user-service-localhost
  serviceName: user
  weight: 10

config:
  id: config-service
  profile: "dev"
  label: "main"